---
import { SITE_NAME } from "../consts";
import { withBase } from "../utils/url";

const heroImageSrc = withBase("/images/hero-placeholder.svg");
const logoSrc = withBase("/favicon.svg");
---

<section class="hero">
  <div class="hero-image-wrap">
    <img
      class="hero-image"
      src={heroImageSrc}
      alt="Suasana Gatherloop — pengunjung sedang main board game"
      width="960"
      height="720"
    />
  </div>

  <div class="brand">
    <img class="logo" src={logoSrc} alt="" width="32" height="32" />
    <span class="brand-name">{SITE_NAME}</span>
  </div>

  <h1 class="tagline">Tempat Nongkrong Seru Sambil Main Board Game</h1>
  <p class="tagline-sub">
    Ratusan pilihan game, kopi enak, dan teman baru menunggumu di {SITE_NAME}.
  </p>
</section>

<style>
  .hero {
    padding-top: 1.5rem;
    text-align: center;
  }

  .hero-image {
    width: 100%;
    height: auto;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-card);
  }

  @media (min-width: 720px) {
    .hero-image-wrap {
      width: 100vw;
      position: relative;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      justify-content: center;
    }

    .hero-image {
      max-width: 960px;
    }
  }

  .brand {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    margin-top: 1.25rem;
  }

  .logo {
    width: 32px;
    height: 32px;
  }

  .brand-name {
    font-size: 1.125rem;
    font-weight: 700;
  }

  .tagline {
    margin-top: 1rem;
    font-size: 1.5rem;
    line-height: 1.25;
  }

  .tagline-sub {
    margin-top: 0.5rem;
    opacity: 0.85;
  }

  @media (min-width: 480px) {
    .tagline {
      font-size: 1.75rem;
    }
  }
</style>
